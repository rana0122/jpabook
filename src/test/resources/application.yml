spring:
#  datasource:
#    url: jdbc:h2:mem:test
#    username: sa
#    password:
#    driver-class-name: org.h2.Driver
#
#  jpa:
#    hibernate:
#      ddl-auto: create-drop #create는 drop하는 초기화를 실행 처음시점에 하고, create-drop은 test 종료 시점에 drop한다.
#    database-platform: org.hibernate.dialect.H2Dialect  # Dialect 명시적 설정
#    properties:
#      hibernate:
#        show_sql: true
#        format_sql: true
logging:
  level:
    org.hibernate.sql: debug
    org.hibernate.orm.jdbc.bind: trace
